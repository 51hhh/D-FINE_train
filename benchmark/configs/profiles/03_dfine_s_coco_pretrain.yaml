# 组合B（主对比-中等）: D-FINE-S，预训练微调
stage: "B_baseline"
purpose: "中等规模预训练基线，用于与 04 对比"
compare_with: "04_rtdetrv2_r34_coco_pretrain.yaml"
next_if_better: "进入 23 验证 Objects365 预训练策略"
profile_name: dfine_s_coco_pretrain
model_type: dfine
repo_dir: third_party/D-FINE
base_config: configs/dfine/custom/dfine_hgnetv2_s_custom.yml
seed: 0
device: "0"
use_amp: true
pretrained: "benchmark/pretrained/dfine/dfine_s_coco.pth" # 已下载
output_group: pairB_dfine_s_pretrain
overrides:
  HGNetv2.pretrained: true
