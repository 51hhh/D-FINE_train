# 扩展组（可选）: D-FINE-M 预训练微调，用于验证更大模型对召回/稳定性的收益
stage: "D_extended"
purpose: "验证 D-FINE 中等偏大模型上限"
compare_with: "03_dfine_s_coco_pretrain.yaml"
next_if_better: "可作为最终候选部署模型"
profile_name: dfine_m_coco_pretrain
model_type: dfine
repo_dir: third_party/D-FINE
base_config: configs/dfine/custom/dfine_hgnetv2_m_custom.yml
seed: 0
device: "0"
use_amp: true
pretrained: "benchmark/pretrained/dfine/dfine_m_coco.pth" # 已下载
output_group: ext_dfine_m_pretrain
overrides:
  HGNetv2.pretrained: true
