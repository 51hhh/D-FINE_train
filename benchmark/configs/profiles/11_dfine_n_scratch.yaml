# 从零训练对照（A组）: D-FINE-N，不加载backbone预训练，不加载检测预训练
stage: "C_scratch_control"
purpose: "验证 D-FINE-N 在本数据集上的预训练收益"
compare_with: "01_dfine_n_coco_pretrain.yaml"
next_if_better: "若优于预训练，可减少预训练依赖"
profile_name: dfine_n_scratch
model_type: dfine
repo_dir: third_party/D-FINE
base_config: configs/dfine/custom/dfine_hgnetv2_n_custom.yml
seed: 0
device: "0"
use_amp: true
pretrained: "" # 保持空，表示不走 -t
output_group: scratch_dfine_n
overrides:
  HGNetv2.pretrained: false
